#include <stdio.h>
#include <math.h>
#include <time.h>
#include <locale.h>

void main()
int n;
printf("Выберите длину загадываемого числа; от 2 до 5\n");
scanf_s(%d, n);
while (n < 2) || (n > 5) {
	printf("Введите длину числа от 2 до 5");
	scanf_s(% d, n);
}
	srand(time(NULL));

