#include <stdio.h> 
#include <math.h>
#include <locale.h>
int main(void) {
	setlocale(LC_ALL, "Rus");
	double h, w, d, pDVP, pDSP, pD, q, CHECK;
	pDVP = 0.00085; //кг на кубический сантиметр
	pDSP = 0.00065;
	pD = 0.00045;
	q = (h - (1,5 * 2)) % 41, 5;
	CHECK = 41 % 20	
		printf (CHECK)







}